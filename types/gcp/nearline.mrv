#lang marv

# Playing....
type _nearlineb = {
  create(cfg) = { name = cfg.name storageClass = "NEARLINE" }
  read(cfg) = { name = cfg.name }
  update(cfg) = { name = cfg.name}
}

type _policy1 = {
  create(cfg) = { name = cfg.name storageClass = "NEARLINE" }
  read(cfg) = { name = cfg.name }
  update(cfg) = { name = cfg.name}
}

type nearlineb =  _nearlineb | bucket 