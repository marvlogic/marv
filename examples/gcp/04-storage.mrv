#lang marv

import types/gcp/storage
# import types/gcp/storage as gcp

module main {

    defaults = {
        project = env("MARV_GCP_PROJECT")
        region = env("MARV_GCP_REGION")
        nested = { x = 123 }
    }
    name = defaults.nested.x

    bucket = storage.bucket defaults <- {
        #name = strf("~a-mybucket" defaults.project)
        name="abc"
    }
}
